namespace-component()
  s = ()
  push(s, \' + arg + \') for arg in arguments
  {'component(' + join('\, ', s) + ')'}
    {block}
namespace-has()
  s = ()
  push(s, \' + arg + \') for arg in arguments
  {'has(' + join('\, ', s) + ')'}
    {block}
namespace-when()
  s = ()
  push(s, \' + arg + \') for arg in arguments
  {'when(' + join('\, ', s) + ')'}
    {block}
namespace-is()
  s = ()
  push(s, \' + arg + \') for arg in arguments
  {'is(' + join('\, ', s) + ')'}
    {block}
namespace-inside()
  s = ()
  push(s, \' + arg + \') for arg in arguments
  {'inside(' + join('\, ', s) + ')'}
    {block}
namespace-util()
  s = ()
  push(s, \' + arg + \') for arg in arguments
  {'util(' + join('\, ', s) + ')'}
    {block}
namespace-container()
  s = ()
  push(s, \' + arg + \') for arg in arguments
  {'container(' + join('\, ', s) + ')'}
    {block}
